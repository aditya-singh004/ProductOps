# Legacy Build Module

Small Java module built with Ant to demonstrate familiarity with legacy Java build tooling.

```bash
ant clean test
```

Rails imports the logs in `sample-build-logs/` through `AntBuildLogParserService`.
